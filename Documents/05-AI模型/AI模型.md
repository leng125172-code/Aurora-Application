# 我需要你帮我修改上传

## 前端修改

1. 列表修改
   1. 支持单模型的多文件展示(类似系统信息-已加载程序集的样式和风格，可折叠可展开)
   2. 补充显示和筛选的字段
2. 上传弹窗
    * 运行平台区域 不需要修改
    * 拖拽区域 不需要修改。
    * 模型名称 需要用户输入。
    * 模型描述 不需要修改。
    * 模型转换类型(新增下拉选项,当文件为onnx模型(单个或多个)，且为Linxu + 瑞芯微平台,显示该选项)
      * 自动判断
      * 使用 ONNX 直接运行
      * 转换为 RKNN 运行
      * 转换为 RKLLM
    * 模型标识 不需要修改。
    * 位置标识 不需要修改。
    * 生成条件 不需要修改。
    * 已选文件(移动到末尾)
      * 改为(类似系统信息-已加载程序集的样式和风格,必须展开，不能折叠)
      * 列名 文件名(不带扩展名的) 文件MD5 文件大小(GB/MB/KB/B,自动单位换算，保留两位小数)
      * 展开列表(默认展开)
        * md5校验是否通过
        * 模型类型(全大写的扩展名)
        * 只有当多个文件时，存在文件选择类型，如果是单文件(前端自动填充0)
            >
            > /// <summary>
            > /// 单文件整合模型
            > /// </summary>
            > SingleWholeModel = 0,
            >
            > /// <summary>
            > /// 拆分模型-编码器
            > /// </summary>
            > SplitEncoder = 1,
            >
            > /// <summary>
            > /// 拆分模型-解码器
            > /// </summary>
            > SplitDecoder = 2
3. 编辑页
   1. 模型编辑不变，补充字段
   2. 文件列表
      * 列名 文件名(不带扩展名的) 文件MD5 文件大小(GB/MB/KB/B,自动单位换算，保留两位小数) 文件类型(编辑)

## 数据库修改

1. 数据库结构修改
   * 记录还要是单模型的记录，现在需要改为支持单模型多文件。表需要主从两张表，主表记录模型信息，从表记录文件信息和位置md5等等
2. 字段补充
   * 模型转换类型(只有onnx模型，且为Linxu + 瑞芯微平台，才需要)
   * 模型转换类型(自动判断,只有onnx模型，且为Linxu + 瑞芯微平台，才需要)
   * 文件类型 区分单文件整合模型还是拆分模型

当文件为onnx模型(单个或多个)，且为Linxu + 瑞芯微平台.在上传时

1. 单个模型可能存在一个或多个.onnx文件，要支持多文件上传。
   多文件可能有decoder_model.onnx，encoder_model.onnx(decoder_model_merged.onnx,decoder_with_past_model.onnx)

数据库修改:记录还要是单模型的记录，模型表需要主从两张表，主表记录模型信息，从表记录文件信息和位置md5等等

模型名称需要用户输入(之前是文件名，但是现在看并不合适)。
上传后合并分片和命名文件名就用模型名称模型名称+时间码,如果是上传多个文件还要拼接+“_{序号}”
多个文件必定是拆分的，需要选择类型，如果是单文件(前端自动填充0)
/// <summary>
/// 单文件整合模型：model.onnx
/// </summary>
SingleWholeModel = 0,

/// <summary>
/// 拆分模型-编码器：encoder_model.onnx
/// </summary>
SplitEncoder = 1,

/// <summary>
/// 拆分模型-解码器：decoder_model.onnx
/// </summary>
SplitDecoder = 2

注意，模型还是只支持单个，文件支持多个。

1. 后端 获取模型列表，添加两个字段
   当文件为onnx模型(单个或多个)，且为Linxu + 瑞芯微平台.在上传时
