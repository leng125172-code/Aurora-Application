# 第三方工作流前端接口变更说明

> 更新时间：2026-07-27  
> 接口前缀：`/api/app/workflow`  
> TypeScript 客户端：`Sources/AuroraStruct3D/AuroraStruct3D.Frontend/src/api/workflow.ts`

## 1. 变更概述

工作流后端已调整为“GraphData 负责编辑、已保存程序负责执行”的模式。

第三方前端仍通过 `GraphData` 创建和编辑工作流，不需要处理工作流伪代码。新增独立的 `workflow.ts` 客户端作为第三方稳定入口，包含：

- 工作流 CRUD、校验、模拟和输出路径查询。
- 单工作流正式执行及状态查询。
- 项目运行查询和取消。
- 项目任务配置。
- 项目部署、激活、重新激活、回滚和删除。

以下能力不属于第三方接口：

- `SourceCode`、脚本转换和语言服务。
- 草稿和源码历史版本。
- Monaco 编辑器能力。
- 断点、Watch、Trace、单步和其他调试接口。
- `/api/app/workflow/source/*` 下的全部接口。

## 2. 接入方式

项目内前端直接引用：

```ts
import {
    createWorkflow,
    executeWorkflow,
    getExecutionStatus,
    getOutputPaths,
    type WorkflowGraph,
} from '@/api/workflow'
```

独立第三方项目可以复制 `workflow.ts` 的类型和方法，并将其中的 `httpClient` 替换为自身 Axios 实例。请求需要沿用平台现有的认证信息和 JSON 请求头。

## 3. GraphData 数据结构

```ts
export interface WorkflowGraph {
    nodes: Array<Record<string, unknown> & {
        id: string
        type: string
    }>
    edges: Array<Record<string, unknown> & {
        id: string
    }>
}

export interface WorkflowPayload {
    projectId: string
    name: string
    graphData: WorkflowGraph
}
```

节点和边允许携带算子参数、端口、坐标、文本、条件分支和容器子图等扩展字段。第三方前端应保留无法识别的字段，避免读取后再次保存时丢失工作流信息。

## 4. 工作流接口

| 客户端方法 | HTTP | 路径 | 说明 |
|---|---|---|---|
| `listWorkflows(projectId)` | GET | `/api/app/workflow?projectId={projectId}` | 查询项目工作流 |
| `getWorkflow(id)` | GET | `/api/app/workflow/{id}` | 查询工作流及 GraphData |
| `createWorkflow(payload)` | POST | `/api/app/workflow` | 创建工作流 |
| `updateWorkflow(id, payload)` | PUT | `/api/app/workflow/{id}` | 更新工作流 |
| `deleteWorkflow(id)` | DELETE | `/api/app/workflow/{id}` | 删除工作流 |
| `validateWorkflow(id)` | POST | `/api/app/workflow/{id}/validate` | 校验已保存工作流 |
| `simulateWorkflow(id)` | POST | `/api/app/workflow/{id}/simulate` | 模拟已保存工作流 |
| `getOutputPaths(id, variableName)` | GET | `/api/app/workflow/{id}/output-paths?variableName={name}` | 查询模板可用输出路径 |

创建示例：

```ts
const workflow = await createWorkflow({
    projectId,
    name: '检测流程',
    graphData: {
        nodes: [
            { id: 'start', type: 'start-node' },
            { id: 'detect', type: 'operator-node' },
            { id: 'end', type: 'end-node' },
        ],
        edges: [],
    },
})
```

保存时，服务端会完成 GraphData 校验、生成受限 C# 风格脚本、计算哈希并预编译程序。第三方不需要提交或保存 `SourceCode`。

## 5. 输出路径下探

`getOutputPaths` 用于取得可直接用于模板渲染的变量路径。

假设结束节点返回变量 `retstatus`，其结构包含 `status` 和 `regions`，接口可以返回：

```text
retstatus
retstatus.status
retstatus.regions
retstatus.regions[0].name
```

数组路径同时区分：

- 实例路径：`retstatus.regions[0].name`
- Schema 路径：`retstatus.regions[].name`

每个路径项可能包含以下信息：

| 字段 | 说明 |
|---|---|
| `path` | 实例访问路径 |
| `schemaPath` | Schema 路径 |
| `type` | 值类型 |
| `nullable` | 是否允许空值 |
| `isArray` | 是否为数组 |
| `description` | 字段说明 |
| `templateAvailable` | 是否可直接用于模板 |

前端渲染前应处理可空字段；数组 Schema 路径用于选择字段，实际渲染时应使用带下标的实例路径。

## 6. 正式运行接口

### 6.1 执行单个工作流

```ts
const result = await executeWorkflow({
    projectId,
    workflowId,
    mode: 0,
    loopCount: 1,
    inputVariableKeys: [],
    outputVariableNames: ['retstatus.status'],
})
```

请求：

```http
POST /api/app/workflow/executions
```

第三方客户端只允许正式运行模式：

- `0`：单次完整执行（RunOnce）。
- `2`：按照 `loopCount` 指定次数循环执行（Loop）。
- 调试模式不通过第三方模块开放。

工作流必须已经成功保存并具有有效的 `ProgramHash`。旧 GraphData 数据需要先由系统迁移，正式运行不会在执行时临时解析 GraphData。

### 6.2 查询执行状态

```ts
const status = await getExecutionStatus(result.executionId)
```

```http
GET /api/app/workflow/executions/{executionId}?includeVariables=true
```

主要字段：

```ts
interface WorkflowExecutionStatus {
    executionId: string
    workflowId: string
    workflowName: string
    status: number
    error?: boolean
    errorCode?: string
    message?: string
    variables?: Array<Record<string, unknown>>
}
```

图像、点云和 Region 不直接作为大型 JSON 返回。响应只包含摘要或已有文件引用，实际数据继续通过平台现有图像、点云或文件接口访问。

## 7. 项目任务与部署

### 7.1 项目任务

| 客户端方法 | HTTP | 路径 |
|---|---|---|
| `getProjectTasks(projectId)` | GET | `/api/app/workflow/projects/{projectId}/tasks` |
| `updateProjectTasks(input)` | PUT | `/api/app/workflow/projects/tasks` |

任务配置结构：

```ts
interface ProjectTaskBatch {
    projectId: string
    taskType: number
    cycleIntervalSeconds?: number
    items: Array<{
        id: string
        projectId: string
        workflowId: string
        workflowName: string
        isEnabled: boolean
        orderNo: number
    }>
}
```

### 7.2 项目部署

| 客户端方法 | HTTP | 路径 |
|---|---|---|
| `getProjectDeployments(projectId)` | GET | `/api/app/workflow/projects/{projectId}/deployments` |
| `publishProjectDeployment(projectId)` | POST | `/api/app/workflow/projects/{projectId}/deployments` |
| `activateProjectDeployment(deploymentId)` | POST | `/api/app/workflow/deployments/{deploymentId}/activate` |
| `reactivateProjectDeployment(deploymentId)` | POST | `/api/app/workflow/deployments/{deploymentId}/reactivate` |
| `rollbackProjectDeployment(deploymentId)` | POST | `/api/app/workflow/deployments/{deploymentId}/rollback` |
| `deleteProjectDeployment(deploymentId)` | DELETE | `/api/app/workflow/deployments/{deploymentId}` |

发布部署时服务端会校验并预热全部工作流。任何工作流无有效程序或预热失败时，部署不会部分激活。

## 8. 项目运行

| 客户端方法 | HTTP | 路径 |
|---|---|---|
| `getProjectRuns(input)` | GET | `/api/app/workflow/runs` |
| `getProjectRunStatus(runId)` | GET | `/api/app/workflow/runs/{runId}` |
| `cancelProjectRun(runId)` | POST | `/api/app/workflow/runs/{runId}/cancel` |

查询示例：

```ts
const runs = await getProjectRuns({
    projectId,
    skipCount: 0,
    maxResultCount: 20,
})
```

项目运行基于已激活部署的冻结快照执行，不读取当前画布的未保存修改。

## 9. 错误处理

### 9.1 常见状态码

| 状态码 | 处理建议 |
|---|---|
| `400` | 请求字段、GraphData 或工作流校验失败，显示服务端诊断 |
| `401/403` | 登录状态或权限不足 |
| `404` | 工作流、运行或部署不存在 |
| `409` | 数据已被修改或无法安全合并，重新加载最新工作流 |
| `500` | 服务端执行故障，记录 `errorCode` 和 `message` |

### 9.2 运行限制错误码

| 错误码 | 说明 |
|---|---|
| `WORKFLOW_TIMEOUT` | 工作流总执行时间超限 |
| `WORKFLOW_NODE_TIMEOUT` | 单节点执行时间超限 |
| `WORKFLOW_LOOP_LIMIT` | 循环次数超过配置上限 |
| `WORKFLOW_OUTPUT_TOO_LARGE` | 普通结构化输出超过大小限制 |
| `WORKFLOW_PROJECT_QUOTA` | 本机项目调试会话达到上限；第三方正式运行通常不会遇到 |

第三方调用 CRUD 接口通常不会提交并发戳。若后续接入带修订号的内部编辑接口，HTTP 409 响应会附带：

```json
{
  "serverRevision": 12,
  "contentHash": "...",
  "semanticHash": "...",
  "programHash": "..."
}
```

收到 409 后应重新调用 `getWorkflow(id)`，不得自动覆盖服务端版本。

## 10. 本次迁移检查清单

- 将第三方工作流调用统一切换到 `src/api/workflow.ts`。
- 继续以完整 GraphData 创建和更新工作流，并保留未知节点/边字段。
- 不再调用草稿接口：`/{id}/source/draft`。
- 不调用 `/source/*`、源码版本和调试接口。
- 工作流保存成功后再执行或发布部署。
- 模板字段通过 `getOutputPaths` 选择，不在前端自行猜测嵌套 JSON 路径。
- 正式运行只使用 `executeWorkflow`，不传调试模式。
- Release 前端不依赖 Monaco 或工作流 Debug Demo。
