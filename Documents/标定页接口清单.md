# 标定页接口清单

本文档基于前端实际调用整理，来源：

- Sources/AuroraStruct3D/AuroraStruct3D.Frontend/src/api/calibration.ts
- Sources/AuroraStruct3D/AuroraStruct3D.Frontend/src/api/calib-photo.ts

## 1. 标定项目（Calib Project）

基础路径：`/api/app/calib-project`

| 方法 | 路径 | 说明 | 主要参数 |
|---|---|---|---|
| GET | `/api/app/calib-project` | 分页查询标定项目列表 | Query: `filter, deviceSeries, deviceType, calibStatus, startTime, endTime, sorting, skipCount, maxResultCount` |
| GET | `/api/app/calib-project/{id}` | 获取单个标定项目 | Path: `id` |
| POST | `/api/app/calib-project` | 新增标定项目 | Body: `CreateCalibProjectInput` |
| PUT | `/api/app/calib-project/{id}` | 修改标定项目 | Path: `id`，Body: `UpdateCalibProjectInput` |
| DELETE | `/api/app/calib-project/{id}` | 删除标定项目 | Path: `id` |

---

## 2. 标定电机参数（Calib Motor Param）

基础路径：`/api/app/calib-motor-param`

| 方法 | 路径 | 说明 | 主要参数 |
|---|---|---|---|
| GET | `/api/app/calib-motor-param` | 获取标定项目电机参数列表 | Query: `calibProjectId` |
| POST | `/api/app/calib-motor-param/save` | 保存（Upsert）电机参数 | Body: `SaveCalibMotorParamInput` |
| DELETE | `/api/app/calib-motor-param/{id}` | 删除电机参数 | Path: `id` |

---

## 3. 标定相机参数（Calib Camera Param）

基础路径：`/api/app/calib-camera-param`

| 方法 | 路径 | 说明 | 主要参数 |
|---|---|---|---|
| GET | `/api/app/calib-camera-param` | 获取标定项目相机参数列表 | Query: `calibProjectId` |
| POST | `/api/app/calib-camera-param/save` | 保存（Upsert）相机参数 | Body: `SaveCalibCameraParamInput` |
| GET | `/api/app/calib-camera-param/validate-step2` | 校验 Step 2 是否配置完成 | Query: `calibProjectId` |
| GET | `/api/app/calib-camera-param/camera-motor-angle` | 读取左/右相机绑定电机当前单圈角度 | Query: `calibProjectId`, `isMain` |
| POST | `/api/app/calib-camera-param/move-camera-motor` | 移动相机绑定瓴控电机到目标角度 | Body: `MoveCameraMotorInput` |

---

## 4. 标定拍照与计算（Calib Photo / Step5）

基础路径：`/api/app/calib-photo`

### 4.1 棋盘格配置

| 方法 | 路径 | 说明 | 主要参数 |
|---|---|---|---|
| PUT | `/api/app/calib-photo/board-config` | 更新棋盘格配置 | Body: `UpdateBoardConfigInput`（含 `calibProjectId`） |
| GET | `/api/app/calib-photo/board-config/{calibProjectId}` | 获取棋盘格配置 | Path: `calibProjectId` |

### 4.2 拍照接口

| 方法 | 路径 | 说明 | 主要参数 |
|---|---|---|---|
| POST | `/api/app/calib-photo/take-intrinsic-photo` | 内参拍照 | Body: `TakeIntrinsicPhotoInput` |
| POST | `/api/app/calib-photo/take-extrinsic-photo` | 外参拍照 | Body: `TakeExtrinsicPhotoInput` |
| POST | `/api/app/calib-photo/take-stereo-extrinsic-pair-photo` | 双目成对拍照 | Body: `TakeStereoExtrinsicPairPhotoInput` |

### 4.3 照片管理

| 方法 | 路径 | 说明 | 主要参数 |
|---|---|---|---|
| GET | `/api/app/calib-photo/photo-list` | 查询照片列表 | Query: `calibProjectId, cameraDeviceId, photoType?` |
| DELETE | `/api/app/calib-photo/{photoId}` | 删除单张照片 | Path: `photoId` |
| DELETE | `/api/app/calib-photo/invalid-photos` | 删除无效照片 | Query: `calibProjectId, cameraDeviceId` |

### 4.4 计算与状态

| 方法 | 路径 | 说明 | 主要参数 |
|---|---|---|---|
| POST | `/api/app/calib-photo/compute-calibration` | 计算单目内外参 | Query: `calibProjectId, cameraDeviceId` |
| GET | `/api/app/calib-photo/camera-status` | 获取相机标定状态 | Query: `calibProjectId, cameraDeviceId` |
| POST | `/api/app/calib-photo/compute-stereo-calibration` | 计算双目联合外参 | Query: `calibProjectId` |
| GET | `/api/app/calib-photo/stereo-status/{calibProjectId}` | 获取双目标定状态 | Path: `calibProjectId` |

---

## 5. 关键返回 DTO（Step5 常用）

- `CalibComputeResultDto`
  - `intrinsicMatrixJson`
  - `distCoeffsJson`
  - `reprojectionError`
  - `projectorReprojectionError`
  - `extrinsicRvecJson`
  - `extrinsicTvecJson`
- `CalibStereoComputeResultDto`
  - `mainCameraDeviceId`
  - `secondaryCameraDeviceId`
  - `stereoReprojectionError`
  - `rotationMatrixJson`
  - `translationVectorJson`
  - `transformLtoRJson`
  - `transformRtoLJson`
  - `rectificationR1Json`
  - `rectificationR2Json`
  - `projectionP1Json`
  - `projectionP2Json`
  - `rectifyMapWidth`
  - `rectifyMapHeight`
  - `map1XBlobKey`
  - `map1YBlobKey`
  - `map2XBlobKey`
  - `map2YBlobKey`
- `CalibCameraStatusDto`
  - `cameraDeviceId`
  - `intrinsicTotal`
  - `intrinsicValid`
  - `extrinsicTotal`
  - `extrinsicValid`
  - `latestResult`
- `CalibStereoStatusDto`
  - `pairTotal`
  - `pairValid`
  - `latestResult`

---

## 6. 路由注意事项

1. ABP 约定路由下，部分单个 `*Id` 参数会进入路径段（例如 `board-config/{calibProjectId}`、`stereo-status/{calibProjectId}`），不是 query。
2. 前端计算接口已设置较长超时：`compute-calibration`、`compute-stereo-calibration` 默认 120000ms。
3. 若后端抛出 `UserFriendlyException`，HTTP 通常返回业务错误体（非 2xx），前端需按错误消息提示。

---

## 7. Step6 在线扫描完整流程细化

### 7.1 双目无光预览模式

1. 模式初始化：关闭所有结构光投影光源，双相机同步开启。
2. 帧率调度：拉取程序兼容硬件最大同步帧率，左右相机严格同帧曝光、同步采图。
3. 深度运算：实时执行双目立体匹配算法，生成稠密深度图。
4. 图像渲染：深度值映射伪彩色梯度（近暖远冷 / 自定义色阶），叠加原始纹理得到带深度色彩融合图。
5. 前端推送：帧数据编码后实时流式下发前端展示，附带帧率、深度有效率、视差置信度状态。

### 7.2 单目一光（单相机 + 结构光投影仪）

1. 硬件上电：开启结构光投影光源，切换投影图案为标准棋盘格标定图案。
2. 第一轮采集：固定棋盘格尺寸，拍摄第一张彩色原图。
3. 第二轮采集：投影仪参数微调，缩放/微调棋盘格方格尺寸，拍摄第二张彩色原图。
4. 第三轮采集：再次变更棋盘格尺度，拍摄第三张彩色原图。
5. 多尺度棋盘格作用：覆盖远近不同景深区域，提升相位匹配、三维重建鲁棒性。
6. 重建计算：基于多帧结构光相位/棋盘特征解算像素三维坐标，生成深度图。
7. 输出渲染：深度映射彩色贴图，实时推送带深度色彩效果图至前端。

### 7.3 双目一光（双相机 + 单台结构光投影仪）

流程逻辑与单目一光采集步骤完全对齐，区别在于采图单元：

1. 开灯切棋盘格图案，双相机同步采集第一组左右彩图。
2. 调整棋盘格尺寸，同步采集第二组左右彩图。
3. 再次调整棋盘格尺寸，同步采集第三组左右彩图。
4. 融合重建：同时利用双目视差约束 + 结构光相位约束联合解算三维点云。
5. 渲染输出：点云深度上色生成融合彩图，实时流式推送到前端界面。

### 7.4 可实现性结论

可以实现，且可复用现有能力：

1. 相机预览与推流：复用相机实时预览服务与 SignalR 通道。
2. 投影仪控制：复用开灯/关灯、显示模式、棋盘格尺寸调节能力。
3. 标定参数：复用 Step5 的内外参结果、双目校正参数与 map 结果。

### 7.5 研发实现起步建议

1. 第一阶段：先打通在线扫描会话生命周期（启动、停止、状态查询、前端推流）。
2. 第二阶段：接入双目无光深度链路（立体校正 + 视差 + 伪彩渲染）。
3. 第三阶段：接入单目一光与双目一光多尺度采集编排与融合重建。
4. 第四阶段：补齐质量指标上报（fps、深度有效率、置信度、重建耗时）。
