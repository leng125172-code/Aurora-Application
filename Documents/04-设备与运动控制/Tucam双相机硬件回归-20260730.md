# Tucam 双相机硬件回归

- 时间：2026-07-30 13:16–13:21（Asia/Shanghai）
- 环境：独立 Windows 开发机
- 相机：2 × Libra 3405UC
- 程序集：`AuroraStruct3D.Cameras`
- 自动回归结论：通过

## 已通过

- 两台相机稳定硬件 ID 非空、唯一，关闭及 SDK 重初始化后重新扫描身份一致。
- 每台相机完成 20 次打开/关闭循环。
- 每台相机完成 100 帧连续采集、20 次快照、20 次软件触发。
- 每台相机 GenICam NodeMap 按需加载成功，枚举到 150 个节点。
- 双相机顺序采集完成，图像均为 2448×2048，帧签名不同，未发现串台。
- 测试结束后曝光、增益和触发参数执行恢复，SDK 正常反初始化。
- 独立 PostgreSQL 数据库完成全部迁移 Up、通用相机迁移 Down/Up，并通过本地 Host 启动与种子数据初始化。
- 本地 Host 后台扫描发现 2 台、冲突 0 台、打开 2 台；认证后设备列表按数据库 ID 返回正确的 `tucam + HardwareId` 绑定。
- 两台相机分别经 HTTP 快照接口成功返回 JPEG Data URI（约 20 MB），验证了 HTTP → 数据库设备 ID → 驱动注册表 → 真机链路。

## 回归中发现并修复

- 原 Open 后自动 NodeMap 预热会与关闭、重扫形成原生 SDK 竞态；高频双机生命周期回归曾触发 `AccessViolationException`。
- NodeMap 已改为参数面板首次访问时按需加载，修复后完整回归无崩溃。
- `Libra 3405UC` 不暴露温度节点，扫描能力不再错误声明 `Temperature`。
- 设备信息接口原先会因缺少温度能力整体失败，现改为温度字段返回 `null`、其他基础信息正常返回。

## 尚未解除的发布门禁

- 17:41–17:46 在人工交换 USB 连接后完成单实例全量复测，全部用例通过；两台相机身份哈希与交换前一致，未发生错绑或串台。
- 本次换口后 Tucam SDK 枚举仍为 `***5003 → 0`、`***5008 → 1`，未实际制造出索引对调，因此“索引变化后的重绑定”场景尚不能据此宣告覆盖。
- 回归工具已增加跨进程单实例锁，后续若已有回归运行会直接拒绝第二个进程，避免多个进程同时控制厂商 SDK。
- HTTP 登录、后台扫描、设备绑定、设备信息与双机快照链路已通过；SignalR 预览和标定采图仍待专门回归。
- Swagger JSON 当前被既有 `WorkflowAppService.GetListAsync` HTTP 方法歧义阻断；ABP API Definition 正常生成，已用于核对相机路由。此项不影响相机 HTTP 调用，但应单独修复。
- `Libra 3405UC` 未采集时设备信息中的 `CurrentWidth/CurrentHeight` 仍返回 0；真实采集帧已确认 2448×2048。该展示问题不影响采集，但需后续按驱动提供可靠的静态尺寸来源。
- USB 索引重排、SignalR 预览和标定采图完成前，不更新承担联调任务的测试设备。

## USB 交换后复测

- 有效报告：`tucam-regression-20260730-174155.json`
- 结论：PASS，失败用例 0。
- 两台相机再次通过各 20 次生命周期循环、100 帧采集、20 次快照、20 次软件触发、NodeMap、重扫和双机顺序采图。
- 期间曾因测试命令超时后子进程仍运行而误启动第二实例，产生两份 SDK 争用失败报告；确认原因后已作废，随后单实例完整复测通过。

## 复测命令

```powershell
dotnet run --project Tools/TucamPerformanceTester/TucamPerformanceTester.csproj -- --regression --output "$env:TEMP\AuroraStruct3D-TucamRegression"
```
